package models.Employees;

import models.Employees.Info.Address;
import models.Employees.Info.Sindicate;

public class Monthly extends Employee {


    public Monthly(int id, String name, Address address, Sindicate sindicate){
        super(id, name, address, sindicate);
    }
}
